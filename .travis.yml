language: node_js
node_js:
  - 6
services:
  - postgresql
before_script:
  - psql -c 'create database quizwiz_travis_db;' -U postgres
# addons:
#   postgresql: "9.5"
script:
  - npm run travis-test
# dist: trusty
# sudo: required


